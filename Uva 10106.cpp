#include<bits/stdc++.h>
using namespace std;
void printVector(vector<ll> v)
{
    for(int i=0;i<v.size();i++)
        cout << v[i] << " ";
    cout << endl;
}
int sub(int a, int b)
{
    if(a>=b)
      return a-b;
    else
      return b-a;
}
int big(int x,int y)
{
    if(x>y)
        return x;
    else
        return y;
}
int main()
{
    string str,str2;
    while(cin >> str >> str2)
    {
        int len=str.length();
        int len2=str.length();
        if(len2==len)
        {

        }
    }
}
